<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:paddingHorizontal="20dp"
    android:paddingTop="50dp">

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:text="Mood Stat"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/textPrimary"
        android:layout_marginBottom="18dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <!-- Calendar Card -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/dialog_bg"
        android:padding="16dp"
        android:elevation="4dp"
        android:layout_marginHorizontal="8dp"
        android:layout_marginBottom="16dp"
        android:clipToOutline="true">

        <!-- Month Navigation -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp">

            <ImageButton
                android:id="@+id/btnPrevMonth"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="8dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_media_previous"
                app:tint="@color/textPrimary"
                android:contentDescription="Previous Month" />

            <TextView
                android:id="@+id/tvMonth"
                android:text="December 2024"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="@color/textPrimary"
                android:layout_marginHorizontal="20dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <ImageButton
                android:id="@+id/btnNextMonth"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="8dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_media_next"
                app:tint="@color/textPrimary"
                android:contentDescription="Next Month" />
        </LinearLayout>

        <!-- Week Day Row -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="8dp"
            android:layout_marginBottom="6dp">

            <TextView android:text="Mo" style="@style/WeekDayText" />
            <TextView android:text="Tu" style="@style/WeekDayText" />
            <TextView android:text="We" style="@style/WeekDayText" />
            <TextView android:text="Th" style="@style/WeekDayText" />
            <TextView android:text="Fr" style="@style/WeekDayText" />
            <TextView android:text="Sa" style="@style/WeekDayText" />
            <TextView android:text="Su" style="@style/WeekDayText" />
        </LinearLayout>

        <!-- Calendar Grid (Larger and clearer) -->
        <GridView
            android:id="@+id/calendarGrid"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:numColumns="7"
            android:horizontalSpacing="10dp"
            android:verticalSpacing="10dp"
            android:gravity="center"
            android:stretchMode="columnWidth"
            android:layout_marginBottom="8dp"
            android:paddingBottom="8dp" />
    </LinearLayout>

    <!-- Info -->
    <TextView
        android:id="@+id/tvMoodInfo"
        android:text="Tap a date to add or view your mood"
        android:textColor="@color/textSecondary"
        android:textSize="14sp"
        android:layout_marginTop="8dp"
        android:textAlignment="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</LinearLayout>
